function Reuse(props)
{
    return(
        <div>
            <span>page 2 Reuse{props.data.name}</span>
            <span>page 2 Reuse{props.data.address}</span>
        </div>
        
    )
}

export default Reuse; 